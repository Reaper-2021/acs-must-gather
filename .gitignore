# Local must-gather captures produced by `oc adm must-gather`
must-gather.local.*/
must-gather/
quay-io-*/
*.must-gather.tar
*.must-gather.tgz

# Python bytecode from the analyzer / tests
__pycache__/
*.pyc
